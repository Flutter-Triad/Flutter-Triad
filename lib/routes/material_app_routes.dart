import 'package:flutter/material.dart';

class MaterialAppRoutes{
  static Map<String, WidgetBuilder> routes(){
    return {
      // TODO: ROUTES
      // Routes.launchScreen : (context) => const LaunchScreen(),
    };
  }
}